server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址
      config:
        server-addr: localhost:8848 #配置Nacos地址
        file-extension: yaml
management:
  endpoints:
    web:
      exposure:
        include: '*'